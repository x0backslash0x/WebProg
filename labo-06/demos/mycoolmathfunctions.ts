/* export function add(num1: number, num2: number): number {
    return num1 + num2;
} */

// default werkt enkel als het de enige functie is in deze module
export default function (num1: number, num2: number): number {
    return num1 + num2;
}